import styled from 'styled-components';

const Box = styled.div`
  border-radius: 12px;
  padding: 16px;
`;

const HomePage = () => {
  return (
    <div>
      <Box>홈입니다요</Box>
    </div>
  );
};

export default HomePage;
